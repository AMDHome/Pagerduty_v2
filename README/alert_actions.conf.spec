[pagerduty]

param.incident_url = <string>
* PagerDuty incident url

param.integration_key = <string>
* Integration key to send the alert incidents
* (required)

param.resolve_keyword = <string>
* Integration key to send the alert incidents
* (required)

param.resolve_keyword_location = [0|1]
* Integration key to send the alert incidents
* (required)